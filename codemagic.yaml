workflows:
  ios-release:
    name: iOS Release
    max_build_duration: 120
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.dhiyohamkids.kids
      vars:
        XCODE_WORKSPACE: "ios/KidsChore.xcworkspace"
        XCODE_SCHEME: "KidsChore"
        BUNDLE_ID: "com.dhiyohamkids.kids"
      node: 20.9.0
      xcode: 16.3
      cocoapods: default
    scripts:
      - name: Install dependencies
        script: |
          npm install

      - name: Install CocoaPods
        script: |
          cd ios && pod install

      - name: Fix gRPC modulemap
        script: |
          GRPC_DIR="ios/Pods/Headers/Private/grpc"
          mkdir -p "$GRPC_DIR"
          if [ ! -f "$GRPC_DIR/gRPC-Core.modulemap" ]; then
            echo 'framework module gRPC_Core { }' > "$GRPC_DIR/gRPC-Core.modulemap"
          fi

      - name: Set up code signing
        script: |
          xcode-project use-profiles

      - name: Fix bundle ID
        script: |
          /usr/libexec/PlistBuddy -c "Set :CFBundleIdentifier com.dhiyohamkids.kids" ios/KidsChore/Info.plist

      - name: Build iOS
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME" \
            --config Release

    artifacts:
      - "*.ipa"
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: true